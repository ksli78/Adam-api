# --- Core dependencies pinned for stability ---
numpy==1.26.4
# --- end pins ---

# PyTorch with CUDA 11.8 support for GPU acceleration
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.1.0+cu118

# ADAM RAG Core Dependencies
# FastAPI web framework and server
fastapi==0.110.1
uvicorn[standard]==0.27.1
pydantic
python-multipart  # Required for file uploads in FastAPI

# Document processing
docling>=2.8,<3  # Structure-aware PDF extraction

# Embedding and ML models
sentence-transformers==3.2.0
transformers==4.48.1
tokenizers
huggingface_hub==0.25.2
safetensors==0.4.5
accelerate==0.34.2  # GPU acceleration for transformers

# Vector database and retrieval
chromadb>=0.4.0
rank_bm25==0.2.2
rapidfuzz==3.9.2  # Optional: Used by chromadb for fuzzy matching (may cause install issues on Windows)

# LLM integration
ollama>=0.1.0

# Natural language processing
nltk>=3.8.1
regex>=2023.0.0

# Configuration
pyyaml>=6.0

# SQL Database connectivity
pyodbc>=4.0.39  # MS SQL Server connector
